# shuiyin-server → shuiyin-edge-server 完整性审计

## 结论

`shuiyin-edge-server` 以 EdgeOne KV、Blob 和边缘函数重新实现传统 Node/SQLite 服务的线上 HTTP 能力。它追求 API 与安全语义兼容，但不是传统服务进程的逐行复制品。

## 已覆盖的线上能力

- 健康检查、CORS 与版本能力声明
- 微信身份登录、退出、绑定码
- 拍摄票据、V2 注册、V2/V3 验真、公开密钥
- 逆地理编码
- 模板目录、详情、下载令牌、模板包、预览、离线租约与续期
- 管理员密码、恢复码、Passkey、TOTP、CSRF、会话、密码移除
- Dashboard、用户目录/备注/停用、管理员、审计
- 模板创建、原子发布、版本发布、启停、归档、删除
- 用户组创建、初始成员、详情、成员增删、安全删除
- 用户和用户组模板授权、撤销及授权数量
- RBAC、模板范围、用户组范围和委派防提权

## EdgeOne 的实现差异

| 传统服务 | EdgeOne 服务 | 影响 |
| --- | --- | --- |
| SQLite 本地事务 | KV 最终一致性 + Blob 条件写 | 跨对象操作采用补偿、幂等和失败关闭，不能宣称数据库级强事务等价 |
| 本地私有模板目录 | EdgeOne Blob | 对外 API 保持一致，运维方式不同 |
| CLI 清理、回填、统计脚本 | 部署外运维任务 | 不属于在线 HTTP API，需要通过独立运维流程执行 |
| 本地管理员 bootstrap CLI | 签名迁移导入 | Edge 环境不运行本地 CLI，管理员通过受签名保护的迁移载入 |
| 进程内速率限制 | 边缘实例内速率限制 | 全局限流仍应由 EdgeOne 安全策略承担 |

## 不可省略的生产验收

1. 密码、Passkey、TOTP 三种登录与会话失效。
2. 用户备注、停用后会话/授权/成员关系撤销。
3. 用户组初始成员、成员移除、空组删除保护。
4. 模板首发、新版本、启停、归档、删除。
5. 用户/用户组授权与撤销、授权计数。
6. 模板下载、预览、离线租约和撤权后的 epoch 失效。
7. V2 注册、V2/V3 验真与公开密钥轮换。
8. Scoped Admin 越权拒绝与审计范围隔离。

只要上述任一项没有真实生产证据，均不得把迁移标记为“完整验收通过”。
