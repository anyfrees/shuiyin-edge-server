import test from "node:test";import assert from "node:assert/strict";import fs from "node:fs";import path from "node:path";import{fileURLToPath}from"node:url";
import{planWorkLogItem,planWorkLogSummary,semanticDraftEnabled,WORK_LOG_CHINESE_PLANNER_VERSION}from"../src/work-log/chinese-draft-planner.generated.js";
const root=path.resolve(path.dirname(fileURLToPath(import.meta.url)),"..");
test("PLANNER-PARITY shared artifact remains byte-identical to Docker",()=>assert.deepEqual(fs.readFileSync(path.join(root,"src/work-log/chinese-draft-planner.generated.js")),fs.readFileSync(path.resolve(root,"../shuiyin-server/src/work-log/chinese-draft-planner.js"))));
test("PLANNER-PARITY D1 EdgeOne SQLite use deterministic text contract",()=>{const items=[{category:"设备巡检",actions:["巡检"],objects:["设备"],locations:["四为楼","四端楼"]},{category:"大屏故障",objects:["大屏"],issues:["大屏故障"],locations:["实验楼"]}],a=JSON.stringify(planWorkLogSummary(items));assert.equal(a,JSON.stringify(planWorkLogSummary(items)));assert.match(a,/记录实验楼大屏故障/);assert.doesNotMatch(a,/相关工作|已处理|恢复正常/);assert.equal(planWorkLogItem(items[0]).plannerVersion,WORK_LOG_CHINESE_PLANNER_VERSION);assert.equal(semanticDraftEnabled({}),false)});
